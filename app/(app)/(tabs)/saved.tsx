import React from "react";
import { SafeAreaView, Text } from "react-native";

export default function Saved() {
  return (
    <SafeAreaView className="flex-1 mx-4 mt-[32px]">
      <Text className=" mt-3 mb-6 text-center">Saved</Text>
    </SafeAreaView>
  );
}
